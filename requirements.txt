# Core dependencies
numpy
matplotlib
refl1d==1.0.0b0
bumps
pydantic
tqdm
click
scipy
fastmcp
pyarrow
pandas
lmfit

# Development dependencies should be installed via:
# pip install -e .[dev]
# which includes: pytest, pytest-cov
